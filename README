Qlarius Online
==============

Qlarius Online is a Spring MVC/Webflow Insurance Application.

To get up and running:

	- create a new MySQL database called qlarius
	- create the database table structure using src/main/resources/schema.sql
	- (if required) create sample users and data using src/main/resources/import.sql
	- build the application using maven (mvn package) and install the resultant WAR file in your container

Default logins are admin/password, fred/password, barney/password.	
	
Peter Raymond (praymond@opentext.com)

